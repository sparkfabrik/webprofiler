{% block toolbar %}
  {% set icon %}
    {{ include('@webprofiler/Icon/blocks.svg') }}
    <span class="sf-toolbar-value">{{ collector.getLoadedEntitiesCount }}</span>
  {% endset %}

  {% set text %}
    <div class="sf-toolbar-info-group">
      <div class="sf-toolbar-info-piece">
        <b>Loaded entities</b>
        <span>{{ collector.getLoadedEntitiesCount }}</span>
      </div>
    </div>
  {% endset %}

  {{ include('@webprofiler/Profiler/toolbar_item.html.twig', { link: profiler_url }) }}
{% endblock %}
